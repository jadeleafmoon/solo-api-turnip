const { expect, assert } = require('chai');
// const { getAll } = require('../routes/api');
const server = 'http://localhost:3030';


describe('getAll', () => {
	it('it should get all products', async () => {
		expect('hello').to.equal('hello');
	});
});
